define(['backbone'], function(Backbone, Controller){

    describe('Testcases Article Controller', function() {
        var controller;

        beforeEach(function(){
            controller = new Controller();
        });

        // it('should have a settings function which return a defaultAuthor', function() {
            // expect(controller.settings('defaultAuthor') instanceof String).toBeTruthy();
        // });
    });

});